[bumpversion]
current_version = 1.1.0.15
parse = (?P<major>\d+)\.(?P<minor>\d+)\.(?P<patch>\d+)\.(?P<release>\d+)
serialize = 
	{major}.{minor}.{patch}.{release}
message = Bump pandas-stubs version: {current_version} -> {new_version}

[bumpversion:file:setup.py]
